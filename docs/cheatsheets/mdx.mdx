---
id: mdx-cheatsheet
title: MDX Cheatsheet
sidebar_label: MDX CheatSheet
description: Essential MDX syntax and examples for building interactive docs
keywords: [mdx, markdown, react, cheatsheet, docusaurus]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Details from '@theme/Details';
import Link from '@docusaurus/Link';

# 📘 MDX Cheatsheet

A quick reference guide to writing MDX – Markdown + JSX. Perfect for Docusaurus, Next.js, and other static site generators that support interactive documentation.

---

## 🔍 What is MDX?

MDX lets you use **JSX (React components)** directly in Markdown files. It's like Markdown supercharged with interactivity.

---

## 📝 Basic Markdown Syntax

```md
# Heading 1

## Heading 2

### Heading 3

**Bold**  
_Italic_  
~~Strikethrough~~

- List Item 1
- List Item 2

[Link](https://example.com)

> This is a blockquote

| Column 1 | Column 2 |
|----------|----------|
| Data 1   | Data 2   |

- [x] Completed task
- [ ] Pending task
```

---

## ⚛️ Using JSX in MDX

```jsx
<h1>This is a JSX header inside MDX</h1>
<MyComponent></MyComponent>
```

---

## 📦 Importing Components

```jsx
import MyComponent from '../components/MyComponent';

<MyComponent></MyComponent>;
```

---

## 💡 Admonitions

```md
:::note
This is a note.
:::

:::tip
This is a tip.
:::

:::info
This is some information.
:::

:::warning
This is a warning.
:::

:::danger
This is a danger alert!
:::
```

| Type       | Syntax Example | Color  |
| ---------- | -------------- | ------ |
| note     | `:::note`       | Gray  |
| Tip     | `:::tip`       | Green  |
| Info    | `:::info`      | Blue   |
| Warning | `:::warning`   | Orange |
| Danger  | `:::danger`    | Red    |

**Output:**
:::note
This is a note.
:::

:::tip
This is a tip.
:::
:::info
This is some information.
:::

:::warning
This is a warning.
:::

:::danger
This is a danger alert!
:::
---

## 🧪 Code Blocks

### Static Code Block

```js
const greet = (name) => `Hello, ${name}`;
```

### Live Code Block (Docusaurus)

```mdx
import LiveCodeBlock from '@theme/LiveCodeBlock';

<LiveCodeBlock language='js'>{`
function Hello() {
  return 'Hi!';
}
console.log(Hello());
`}</LiveCodeBlock>
```

---

## 🧱 Custom Components Example

```jsx
// MyCard.jsx
export default function MyCard({ title, children }) {
  return (
    <div className='p-4 rounded-xl bg-blue-50 shadow'>
      <h3>{title}</h3>
      <div>{children}</div>
    </div>
  );
}
```

Usage in MDX:

```mdx
import MyCard from '../components/MyCard';

<MyCard title='Note'>This is a custom note card.</MyCard>
```

---

## 🧾 Frontmatter

```mdx
---
id: hello-mdx
title: Hello from MDX
sidebar_label: Hello MDX
description: MDX frontmatter example
keywords: [mdx, markdown, react]
tags: [tutorial, guide]
authors: [author-name]
date: 2024-01-15
image: /img/cover.png
---
```

### Common Frontmatter Fields

| Field | Description | Example |
|-------|-------------|---------|
| `id` | Unique identifier | `hello-mdx` |
| `title` | Page title | `Hello from MDX` |
| `sidebar_label` | Sidebar display name | `Hello MDX` |
| `description` | Meta description | `MDX frontmatter example` |
| `keywords` | SEO keywords | `[mdx, markdown, react]` |
| `tags` | Content tags | `[tutorial, guide]` |
| `authors` | Author references | `[author-name]` |
| `date` | Publication date | `2024-01-15` |
| `image` | Featured image | `/img/cover.png` |

---

## 🖼️ Images and Media

### Basic Images

```mdx
![Alt text](/img/example.png "Optional title")
```

### Responsive Images

```jsx
<img 
  src="/img/example.png" 
  alt="Description" 
  style={{maxWidth: '100%', height: 'auto'}}
/>
```

### Image with Caption

```jsx
<div style={{textAlign: 'center'}}>
  <img src="/img/example.png" alt="Description" />
  <p><em>Caption text</em></p>
</div>
```

---

## 🎭 Interactive Examples

### Counter Component

```jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);
  
  return (
    <div style={{textAlign: 'center', padding: '20px'}}>
      <h3>Count: {count}</h3>
      <button 
        onClick={() => setCount(count + 1)}
        style={{
          padding: '10px 20px',
          margin: '5px',
          backgroundColor: '#007acc',
          color: 'white',
          border: 'none',
          borderRadius: '4px',
          cursor: 'pointer'
        }}
      >
        Increment
      </button>
      <button 
        onClick={() => setCount(count - 1)}
        style={{
          padding: '10px 20px',
          margin: '5px',
          backgroundColor: '#dc3545',
          color: 'white',
          border: 'none',
          borderRadius: '4px',
          cursor: 'pointer'
        }}
      >
        Decrement
      </button>
    </div>
  );
}

<Counter></Counter>
```

---

## 📝 Code Examples with Syntax Highlighting

### Multiple Language Support

````mdx
```js title="JavaScript Example"
const greet = (name) => {
  return `Hello, ${name}!`;
};
```

```ts title="TypeScript Example"
const greet = (name: string): string => {
  return `Hello, ${name}!`;
};
```

```python title="Python Example"
def greet(name):
    return f"Hello, {name}!"
```
````

### Code with Line Numbers

````mdx
```js {1,3-5} title="Highlighted Lines"
function example() {
  console.log('Line 1 - highlighted');
  console.log('Line 2 - not highlighted');
  console.log('Line 3 - highlighted');
  console.log('Line 4 - highlighted');
  console.log('Line 5 - highlighted');
}
```
````

---

## 📊 Tables

```md
| Feature | MDX | Markdown |
|---------|-----|----------|
| JSX Support | ✅ | ❌ |
| Components | ✅ | ❌ |
| Interactivity | ✅ | ❌ |
```

**Output:**
| Feature | MDX | Markdown |
|---------|-----|----------|
| JSX Support | ✅ | ❌ |
| Components | ✅ | ❌ |
| Interactivity | ✅ | ❌ |

---

## ✅ Task Lists

```md
- [x] Learn MDX basics
- [x] Create custom components
- [ ] Deploy to production
- [ ] Write documentation
```

**Output:**
- [x] Learn MDX basics
- [x] Create custom components
- [ ] Deploy to production
- [ ] Write documentation

---

## 🎯 Docusaurus-Specific Features

### Tabs

```mdx
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="apple" label="Apple" default>
    This is an apple 🍎
  </TabItem>
  <TabItem value="orange" label="Orange">
    This is an orange 🍊
  </TabItem>
  <TabItem value="banana" label="Banana">
    This is a banana 🍌
  </TabItem>
</Tabs>
```

**Output:**
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="apple" label="Apple" default>
    This is an apple 🍎
  </TabItem>
  <TabItem value="orange" label="Orange">
    This is an orange 🍊
  </TabItem>
  <TabItem value="banana" label="Banana">
    This is a banana 🍌
  </TabItem>
</Tabs>


### Collapsible Sections

```mdx
import Details from '@theme/Details';

<Details>
  <summary>Click to expand</summary>
  <div>
    This content is hidden by default and can be expanded.
  </div>
</Details>
```

### Links and Navigation

```mdx
import Link from '@docusaurus/Link';

<Link to="/docs/intro">Go to Introduction</Link>
<Link to="/blog" target="_blank">External Blog Link</Link>
```

---

## 🎨 Advanced MDX Patterns

### Conditional Rendering

```jsx
import { useState } from 'react';

function ToggleContent() {
  const [show, setShow] = useState(false);
  
  return (
    <div>
      <button onClick={() => setShow(!show)}>
        {show ? 'Hide' : 'Show'} Content
      </button>
      {show && <p>This content toggles!</p>}
    </div>
  );
}

<ToggleContent></ToggleContent>
```

### Dynamic Imports

```jsx
import { useState, useEffect } from 'react';

function DynamicComponent() {
  const [Component, setComponent] = useState(null);
  
  useEffect(() => {
    import('./MyComponent').then(module => {
      setComponent(() => module.default);
    });
  }, []);
  
  return Component ? <Component></Component> : <div>Loading...</div>;
}
```

### MDX with Props

```jsx
// components/InfoBox.jsx
export default function InfoBox({ type = 'info', children }) {
  const colors = {
    info: 'bg-blue-50 border-blue-200',
    warning: 'bg-yellow-50 border-yellow-200',
    error: 'bg-red-50 border-red-200'
  };
  
  return (
    <div className={`p-4 border rounded-lg ${colors[type]}`}>
      {children}
    </div>
  );
}
```

Usage:
```mdx
import InfoBox from '../components/InfoBox';

<InfoBox type="warning">
  This is a warning message!
</InfoBox>
```

---

## 🔧 MDX Configuration

### Docusaurus Config

```js
// docusaurus.config.js
module.exports = {
  presets: [
    [
      '@docusaurus/preset-classic',
      {
        docs: {
          remarkPlugins: [
            // Add remark plugins here
          ],
          rehypePlugins: [
            // Add rehype plugins here
          ],
        },
      },
    ],
  ],
};
```

### Custom MDX Components

```js
// docusaurus.config.js
module.exports = {
  themeConfig: {
    mdx: {
      components: {
        // Global components available in all MDX files
        MyGlobalComponent: require.resolve('./src/components/MyGlobalComponent'),
      },
    },
  },
};
```

---

## 🚀 Best Practices

### 1. Component Organization

```jsx
// ✅ Good: Organized imports
import React from 'react';
import MyComponent from '../components/MyComponent';
import { Button } from '@docusaurus/theme-common';

// ✅ Good: Clear component usage
<MyComponent title="Example"></MyComponent>
<Button variant="primary">Click me</Button>
```

### 2. Performance Optimization

```jsx
// ✅ Good: Lazy loading for heavy components
import { lazy, Suspense } from 'react';

const HeavyComponent = lazy(() => import('./HeavyComponent'));

<Suspense fallback={<div>Loading...</div>}>
  <HeavyComponent></HeavyComponent>
</Suspense>
```

### 3. Accessibility

```jsx
// ✅ Good: Accessible components
<button 
  onClick={handleClick}
  aria-label="Close dialog"
  role="button"
>
  ×
</button>
```

---

## 🐛 Common Issues & Solutions

### Issue: JSX not rendering
**Solution:** Ensure you're using `.mdx` extension, not `.md`

### Issue: Components not found
**Solution:** Check import paths and ensure components are exported correctly

### Issue: Styling not applied
**Solution:** Use `className` instead of `class`, and ensure CSS is properly imported

### Issue: Build errors
**Solution:** Check for syntax errors in JSX and ensure all dependencies are installed

---

## 📚 Resources

- [MDX Official Docs](https://mdxjs.com/)
- [Docusaurus MDX Guide](https://docusaurus.io/docs/markdown-features/mdx)
- [Markdown Guide](https://www.markdownguide.org/)
- [React JSX Documentation](https://reactjs.org/docs/introducing-jsx.html)
- [Docusaurus Components](https://docusaurus.io/docs/advanced/ssg#docusaurus-theme-components)
