---
title: Python Input and Output
description: Read user input, validate data, and print formatted output.
sidebar_position: 7
---

# Python Input and Output

Use `input()` for user input and `print()` for output.

## Input

```python
name = input("Enter your name: ")
```

`input()` returns a string.

## Type Conversion

```python
age = int(input("Enter age: "))
height = float(input("Enter height: "))
```

Handle invalid input with `try/except`.

## Output Formatting with f-strings

```python
print(f"Hello, {name}. You are {age} years old.")
```

## Useful print Options

```python
print("Python", "Docs", sep=" - ", end="\n")
```
